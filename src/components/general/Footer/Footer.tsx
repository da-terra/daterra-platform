import React from "react";
import { StyledWrapper, Logo } from "./styled";

type Props = {};

const Footer: React.FC<Props> = () => {
  return (
    <StyledWrapper>
      <Logo />
    </StyledWrapper>
  );
};

export default Footer;
