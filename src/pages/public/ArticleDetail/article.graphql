query articles($slug: String!) {
  article(slug: $slug) {
    slug
    image {
      src
      alt
      fallbackColor
    }
    author {
      displayName
    }
    title
    richText {
      nodes
    }
    publishDate
  }
}
