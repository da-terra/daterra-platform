type Theme = {
  uuid: string; // unique id is needed to save the theme preference in the localstorage
  backgroundColor: string;
  foregroundColor: string;
  accentColor: string;
};

export default Theme;
