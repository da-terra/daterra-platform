enum RoutePath {
  Home = "/",
  SignIn = "/signin",
  Settings = "/settings",

  // Content pages
  ForBusiness = "/business",
  ForStudents = "/students",
  ForExperts = "/data-scientists"
}

export default RoutePath;
